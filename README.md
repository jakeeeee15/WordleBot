# WordleBot
A web automation code that goes to the wordle website and solves it on its own
It uses python and selenium. I have used a dataset of all 5 letter words and I trained it such that the more wordles it solves the better it gets because it keeps adding words to its vocabular. I run the selenium on a chrome webdriver.
